export class Customer {
    firstName: string;
    lastName: string;
    email: string;
    street: string;
    streetNumber: string;
    zipcode: number;
    countryCode: string;
}

export enum NotifyMedium {
    PHONE,
    EMAIL
}